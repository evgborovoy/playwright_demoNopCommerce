name: Playwright python tests

on:
  push:
    branches: [ 'main', 'master', 'dev' ]
  pull_request:
    branches: [ 'main', 'master', 'dev' ]
  workflow_dispatch:

jobs:
  test:
    name: test (${{ matrix.browser }})
    runs-on: ubuntu-latest
    timeout-minutes: 90

    strategy:
      fail-fast: false
      matrix:
        include:
          - browser: chromium
            browser_channel: chrome
          - browser: firefox
            browser_channel: ""
          - browser: webkit
            browser_channel: ""


    permissions:
      contents: write
      pages: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up python
        uses: actions/setup-python@v5
        with:
          python-version: 3.13
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install playwright browsers and dependencies
        run: python -m playwright install --with-deps chromium firefox webkit

      - name: Run tests (pytest + xdist) for ${{ matrix.browser }}
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p allure-results
          EXTRA=""
          if [ -n "${{ matrix.browser_channel }}" ]; then
            EXTRA="--browser-channel=${{ matrix.browser_channel }}"
          fi
          pytest -n auto --browser "${{ matrix.browser }}" $EXTRA --alluredir=allure-results

  report:
    name: Allure report
    runs-on: ubuntu-latest
    needs: [ test ]

    permissions:
      contents: write
      pages: write
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all allure-results
        uses: actions/download-artifact@v4
        with:
          path: ./_allure

      - name: Merge allure-results
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p allure-results
          cp -r _allure/allure-results-chromium/* allure-results/ 2>/dev/null || true
          cp -r _allure/allure-results-firefox/*  allure-results/ 2>/dev/null || true
          cp -r _allure/allure-results-webkit/*   allure-results/ 2>/dev/null || true
          ls -la allure-results

      - name: Install Allure CLI
        shell: bash
        run: |
          set -euo pipefail
          wget -q https://github.com/allure-framework/allure2/releases/download/2.25.0/allure-2.25.0.zip
          unzip -q allure-2.25.0.zip -d "$HOME/allure"
          echo "$HOME/allure/allure-2.25.0/bin" >> "$GITHUB_PATH"
          allure --version

      - name: Generate Allure report
        if: ${{ always() }}
        shell: bash
        run: |
          if [ -d allure-results ] && [ "$(ls -A allure-results)" ]; then
           allure generate allure-results -o allure-report --clean
          else
           echo "No allure results found. Creating placeholder report."
           mkdir -p allure-report
           cat > allure-report/index.html <<'HTML'
           <!doctype html><meta charset="utf-8">
           <title>Allure report</title>
           <h2>No test results were produced</h2>
           HTML
          fi

      - name: Publish Allure report to gh-pages
        if: ${{ always() }}
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin gh-pages || true
          git checkout gh-pages || git checkout --orphan gh-pages
          git rm -rf . || true
          cp -r allure-report/* .
          git add .
          git commit -m "Update Allure report [skip ci]" || echo "No changes to commit"
          git push origin gh-pages --force

      - name: Upload Allure report as artifact
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: allure-report
          retention-days: 10

      - name: Print Allure report link
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          REPO_NAME="$(echo "$GITHUB_REPOSITORY" | cut -d'/' -f2)"
          echo "✅ Allure report published!"
          echo "🌐 Online report: https://${{ github.repository_owner }}.github.io/${REPO_NAME}/"